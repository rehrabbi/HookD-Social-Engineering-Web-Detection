# Secrets — never commit these
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
*.pyc
.venv/
venv/
env/

# Local SQLite database (runtime data)
*.db
hookd.db

# Generated, IP-specific QR image (regenerate per venue)
hookd_qr.png

# Batch OCR output may contain text from personal screenshots (PII) — keep local
ocr_batch_results.csv

# Uploaded scan files (runtime, ephemeral)
static/uploads/*
!static/uploads/.gitkeep

# Editor / OS
.vscode/
.idea/
.DS_Store
tempCodeRunnerFile.py
